using System;

namespace 编程准则
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             * 标识符规则
             * 标志符是给变量、用户定义的类型和这些类成员指定的名称
             * 标识符区分大小写，所以Inter和inter是不同的变量
             * 规则：
             *  可以包含数字字符，但是必须以字母或下划线开头
             *  不能以关键字作为标识符
             *
             * 如果需要把某一保留字座位关键字，如访问另一语言编写的类
             */
        }
    }
}
